class Solution {
  public:
    int findSum(int n) {
        // return (n*(n+1)/2);
        
        // OR  Using Recursion
        if(n == 0){ 
          return 0;
        }
        return n +findSum(n-1);
        
    }
};
